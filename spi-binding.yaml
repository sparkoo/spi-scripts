apiVersion: apis.kcp.dev/v1alpha1
kind: APIBinding
metadata:
  name: spi
spec:
  reference:
    workspace:
      path: root:redhat-appstudio
      exportName: spi
  permissionClaims:
    - resource: secrets
      state: Accepted
